Flask>=2.0.0
flask-cors>=3.0.0
cryptography>=40.0.0
requests>=2.28.0
colorama>=0.4.0
argon2-cffi>=21.0.0
pyotp>=2.8.0
dnspython>=2.0.0

# Optional dependencies (install separately if needed)
# python-nmap>=0.7.0  # For advanced network scanning (requires nmap installed)
# scikit-learn>=1.0.0  # Not required - malware detector works without it
